我的学习笔记
========
1. WorkBuddy 是 Agent：接收输入 → 靠经验规划 → 输出可用文件。
2. Agent 三件套：模型(脑子) + 记忆(历史) + 工具(手)。
3. 记忆不是模型记住，是代码把每轮对话存进列表再发回。
4. 工具是 Python 函数，模型点名调用，代码执行后把结果塞回对话。
5. 我是大专大三学生，目标是做 Agent 应用开发，正在攒作品集。
